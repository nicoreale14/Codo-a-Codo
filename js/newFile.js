if (telefono.length < 12) {
    alert(
        "El número de teléfono ingresado no es válido. Debe contener al menos diez caracteres numéricos, incluyendo el código de área. Por ejemplo: 11-xxxx-xxxx."
    );
    return;
}
